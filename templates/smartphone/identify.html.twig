{% extends 'visitor.html.twig' %}

{% block title %}
	{{ parent()}}
	- Connexion
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
	{{ encore_entry_link_tags('identify') }}
{% endblock %}

{% block body %}
	<main class="container-fluid">
		<div class="row mt-4">
			<div class="d-flex justify-content-center align-items-center">
				<div class="col-xl-6">
					<div class="main-container bg-light rounded-3 p-4">

						<div class="container-title mb-4">
							<h1 class="fw-bold text-tertiary d-flex justify-content-center align-items-center gap-3">
								{% include 'components/svg/icon_loupe.svg' %}
								Identifier votre Smartphone</h1>
						</div>

						<div class="d-flex flex-column mx-xl-4 m-auto">
							{{ component('formIdentify') }}
						</div>
					</div>
				</div>
			</div>
		</main>
	{% endblock %}